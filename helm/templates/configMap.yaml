apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.deploymentName }}-config
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    {{ .Files.Get "resources/application.yaml" | indent 4 }}
