apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.deploymentName }}-config
  namespace: {{ .Values.namespace }}
data:
  application.yaml: |-
    {{ .File.Get "resources/application.yaml" | indent 4 }}
